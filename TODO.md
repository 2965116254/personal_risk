# TODO 待办事项

## 项目状态

- **版本**: v3.2
- **状态**: 开发中
- **最后更新**: 2026-06-29

---

## 待办事项

### 高优先级

- [ ] **规则D实现**：`risk_assessor.py` 中 D 值目前硬编码为 0，需要实现电网设备风险联动值的实际计算逻辑
- [ ] **规则A支持**：添加规则A（作业任务风险值）的评估逻辑
- [x] **日志轮转**：启动时调用 `CacheManager.clean_old_logs(keep_days=30)` 自动清理旧日志文件
- [x] **单元测试**：`tests/` 目录，覆盖 RiskRuleEngine 全部规则方法（53 个用例，pytest）

### 中优先级

- [ ] **API 服务恢复**：旧版 api.py 已移除，如需对外提供 REST API 需重新实现（FastAPI）
- [ ] **数据库连接池**：DataFetcher 当前使用单连接，高并发场景需引入连接池（如 DBUtils 或 mysql-connector-python 的 pooling）
- [ ] **配置加密**：敏感配置项（数据库密码、LLM API Key）使用环境变量或加密存储
- [ ] **异常处理增强**：完善 LLM 调用失败时的降级策略（当前失败时返回默认值但无重试）
- [ ] **缓存清理策略优化**：当前缓存清理固定 30 天，可改为根据 query_time_range 动态调整

### 低优先级

- [ ] **监控告警**：添加 LLM 调用成功率、评估耗时等监控指标
- [ ] **Docker 环境优化**：确保容器内日志、缓存、输出目录的持久化挂载
- [ ] **CI/CD**：添加持续集成流程（代码检查、自动测试）
- [ ] **风险趋势分析**：基于历史缓存数据分析风险变化趋势
- [ ] **批量导入导出**：支持按作业计划编号批量导入评估

---

## Bug 修复

- [ ] **定时任务错过执行**：调查 APScheduler 在长时间运行后偶尔错过执行的问题
- [ ] **LLM 并发控制**：当前 `max_concurrent` 配置为 50，实际建议 10-15，需确认生产环境配置值

---

## 功能增强

- [ ] **同类型作业经验评分**：rule_engine 中 `calc_same_type_score` 已实现但未在 `_calc_single_ticket` 中调用
- [ ] **规则对比分析**：实现客户填入分值与系统计算分值的对比分析（LLM 场景已配置 `max_tokens_rule_compare`）
- [ ] **消息模板**：支持自定义 elink 消息通知模板

---

## 技术债务

- [ ] **缓存文件格式**：`processed_*.json` 文件缺少版本号字段，未来格式变更可能不兼容
- [ ] **日志统一**：部分模块使用 `print()` 重定向到 logger，部分使用 `logging`，需统一
- [ ] **配置校验**：启动时校验 config.yaml 必要字段是否存在，避免运行时才发现配置缺失

---

## 已完成

- [x] 规则B、C风险评估逻辑实现（含规则引擎 + 大模型双重评估）
- [x] 定时任务调度器实现（9:00/18:00 评估 + 凌晨预计算）
- [x] MCP 服务实现（mcp_zlg 独立模块，按单票查询）
- [x] 大模型智能评估（工作任务高度、作业地段、站内/站外判断、人员ID类型）
- [x] 增量缓存机制（LLM 结果缓存，自动跳过已评估工作票）
- [x] 日预计算任务（凌晨自动执行含 LLM 的完整评估）
- [x] Excel 导出功能（含富文本标红、问题描述、违章代码/条款）
- [x] MinIO 文件上传
- [x] elink 消息通知
- [x] 风险等级判定
- [x] 查询时间范围配置（年份范围 + 固定模式）
- [x] 人员过滤配置（默认过滤词 + 括号内容过滤词）
- [x] 代码重构（模块化：risk_assessor / risk_rule_engine / data_fetcher / data_cache / excel_exporter）
- [x] 大模型评估推理依据记录（日志 + Excel 输出）
- [x] 缓存自动清理（损坏文件自动删除 + 过期文件清理 + 日志/输出清理）
- [x] 测试工具（test_problem_description.py 按编号列表批量评估）
- [x] 动火作业票支持
- [x] 基准关系查询（典型基准风险值）
- [x] 客户填入动态风险分值查询
- [x] Docker 容器化部署

---

## 备注

- 优先级定义：
  - **高优先级**：影响核心功能或存在安全隐患
  - **中优先级**：功能增强或代码质量改进
  - **低优先级**：非核心功能或优化建议